# 📁 QR Code Based File Sharing Server (Python)

A simple and efficient Python-based local file sharing server that allows users to share files over a network using a QR code. The application automatically generates a QR code containing the server URL, which can be scanned by other devices on the same network to access and download files.

---

## 🚀 Features

- 📡 Creates a local HTTP file server
- 📱 Generates QR code for instant access
- 🌐 Share files across devices on the same WiFi network
- 🖥️ Automatically opens QR code in browser
- 📂 Serves files from your system directory
- ⚡ Fast and lightweight with no complex setup

---

## 🛠️ Technologies Used

- Python 3
- http.server (built-in HTTP server)
- socket (network communication)
- socketserver (server framework)
- pyqrcode (QR code generation)
- pypng (PNG support for QR code)
- webbrowser (auto open QR code)



